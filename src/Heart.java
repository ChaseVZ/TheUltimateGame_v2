import processing.core.PImage;
import java.util.List;

public class Heart extends Entity {

    public Heart(String id, Point position, List<PImage> images, int full)
    {
        super(id, position, images, 0);
    }
}
